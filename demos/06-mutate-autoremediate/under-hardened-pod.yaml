apiVersion: v1
kind: Pod
metadata:
  name: legacy-worker
  namespace: default
spec:
  containers:
    - name: worker
      image: registry.internal/jobs/worker:3.0.0
      resources:
        limits:
          cpu: "250m"
          memory: "128Mi"
